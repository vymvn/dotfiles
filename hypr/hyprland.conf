
########################################################################################
AUTOGENERATED HYPR CONFIG.
PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
########################################################################################

#
# Please note not all available settings / options are set here.
# For a full list, see the wiki
#

# autogenerated = 1 # remove this line to remove the warning

# See https://wiki.hyprland.org/Configuring/Monitors/
#          name     resolution    placement  scale
monitor=   eDP-1  , 1920x1080 ,    0x0,   1.25
# monitor=   eDP-1  , 1920x1080 ,    1920x100,   1
monitor=   DP-1   , 1920x1080 ,     0x0,       1
monitor= HDMI-A-2 , 1920x1080 ,     0x0,       1


# unscale XWayland
xwayland {
  force_zero_scaling = true
}

# sets xwayland scale
# exec-once=xprop -root -f _XWAYLAND_GLOBAL_OUTPUT_SCALE 32c -set _XWAYLAND_GLOBAL_OUTPUT_SCALE 0.9

# GTK themes n stuff
# exec-one=/home/vymvn/.config/import-gsettings.sh

# toolkit-specific scale
# env = GDK_SCALE,1.25

# env = QT_QPA_PLATFORM,wayland
env = QT_QPA_PLATFORMTHEME,qt6ct
# env = QT_STYLE_OVERRIDE,kvantum

exec-once=waypaper --restore
exec-once=hyprctl setcursor Bibata-Modern-Classic 24

# Some default env vars.
env = GDK_BACKEND=wayland,x11

# polkit agent
# exec-once=/usr/lib/polkit-kde-authentication-agent-1

exec-once=dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP


# Window Rules
windowrule = workspace 2 silent,LibreWolf
# windowrule = workspace 7 silent,KeePassXC
windowrule = workspace 9 silent,discord
windowrule = workspace 10 silent,Spotify

# firefox Picture-in-Picture
windowrulev2 = float,class:^(firefox)$,title:^(Picture-in-Picture)$
windowrulev2 = float,class:^(LibreWolf)$,title:^(Picture-in-Picture)$
windowrulev2 = pin,class:^(firefox)$,title:^(Picture-in-Picture)$
windowrulev2 = pin,class:^(LibreWolf)$,title:^(Picture-in-Picture)$

# For chromium browsers
windowrule = float,title:^(Picture in picture)$
windowrule = pin,title:^(Picture in picture)$


# firefox figma micro indicator
windowrulev2 = nofullscreenrequest,class:^(firefox)$,title:^(Firefox — Sharing Indicator)$
windowrulev2 = float,class:^(firefox)$,title:^(Firefox — Sharing Indicator)$

# common modals
windowrule = float,title:^(Open)$
windowrule = float,title:^(Choose Files)$
windowrule = float,title:^(File Upload)$
windowrule = float,title:^(Open Files)$
windowrule = float,title:^(Save As)$
windowrule = float,title:^(Confirm to replace files)$
windowrule = float,title:^(File Operation Progress)$

windowrule = float, Lxappearance

windowrule = center,pavucontrol
windowrule = float,pavucontrol

windowrule = center,KeePassXC
windowrule = float,KeePassXC


# windowrule = float,com.nextcloud.desktopclient.nextcloud


windowrule = center,feh
windowrule = float,feh


# Layer rules
layerrule = blur, rofi
layerrule = ignorezero, waybar
# layerrule = blur, waybar
layerrule = blur, notifications

# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
# exec-once = hyprpaper
# exec-once = swaybg
# exec-once = keepassxc
# exec-once = nextcloud
# exec-once = dropbox
exec = killall waybar;waybar


exec-once = swayidle -w timeout 10 'if pgrep -x swaylock; then hyprctl dispatch dpms off; fi' resume 'hyprctl dispatch dpms on'
exec-once = swayidle -w timeout 180 'swaylock --indicator-idle-visible --fade-in 6 --grace 7' timeout 200 'hyprctl dispatch dpms off' resume 'hyprctl dispatch dpms on' before-sleep 'swaylock --indicator'
# exec-once = swayidle -w timeout 180 'swaylock --indicator-idle-visible --fade-in 7 --grace 8' before-sleep 'swaylock --indicator'
# exec-once = swayidle -w timeout 180 'swaylock --indicator-idle-visible --fade-in 4 --grace 5' before-sleep 'swaylock --indicator'

# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf


# fn buttons
binde=,XF86AudioLowerVolume,  exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
binde=,XF86AudioRaiseVolume,  exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bind =,XF86AudioMute,         exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bind =,XF86AudioMicMute,      exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
binde=,XF86MonBrightnessUp,   exec, brillo -q -u 150000 -A 5
binde=,XF86MonBrightnessDown, exec, brillo -q -u 150000 -U 5
bind =,XF86AudioPlay,         exec, playerctl play-pause
bind =,XF86AudioPrev,         exec, playerctl previous
bind =,XF86AudioNext,         exec, playerctl next

# Music controls
bind = ctrl, bracketleft, exec, playerctl previous
bind = ctrl, bracketright, exec, playerctl next
bind = ctrl, backslash, exec, playerctl play-pause


# Connect to bluetooth headphones
bind = ctrl, B, exec, bluetoothctl connect AC:12:2F:EF:BD:57


# rules below would make the specific app transparent
# windowrulev2 = opacity 0.9 0.8,class:^(Alacritty)$
windowrulev2 = opacity 0.9 0.8,class:^(kitty)$
# windowrulev2 = opacity 0.8,class:^(discord)$
# windowrule = opaque,class:^(Alacritty)$
# windowrulev2 = opacity 0.8 0.8,class:^(thunar)$


misc {
  vfr = true
  disable_hyprland_logo = true
  disable_splash_rendering = true
}

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = us,ara
    kb_variant =
    kb_model =
    kb_options = grp:shifts_toggle # Both shifts together
    kb_rules =
    # force_no_accel = true

    follow_mouse = 1

    touchpad {
        natural_scroll = yes
    }

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 3
    gaps_out = 5
    border_size = 2
    # col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    # col.active_border = rgba(af2d2df5) rgba(9a9a9aff) 45deg
    # col.active_border = rgba(f28facff) rgba(aae8b1ff) 45deg
    # col.inactive_border = rgba(595959aa)
    
    # Catppuccin
    # col.active_border = rgba(f28facff) rgba(96CDFBff) 45deg
    # col.inactive_border = rgba(9a9a9aff)

    # Dracula
    col.active_border = rgb(44475a) rgb(bd93f9) 90deg
    col.inactive_border = rgba(44475aaa)
    # col.group_border = rgba(282a36dd)
    # col.group_border_active = rgb(bd93f9) rgb(44475a) 90deg

	no_border_on_floating = false

    layout = dwindle
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 5
    blur {
        enabled = true
        size = 2
        passes = 4
        noise = 0.0118
        contrast = 0.98
        brightness = 0.7
        # xray = true
    }
    
    # drop_shadow = no
    drop_shadow = no
    # shadow_range = 4
    shadow_range = 60
    shadow_offset = 1 2 # From dracula
    shadow_scale = 0.97 # From dracula
    shadow_render_power = 3

    # Catppuccin
    # col.shadow = rgba(1a1a1aee)

    # Dracula
    col.shadow = rgba(1E202966)
}

animations {
    enabled = yes

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05
    bezier = linear, 0.0, 0.0, 1.0, 1.0

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    # animation = border, 1, 10, default
    # animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default

    # Border animation
    # animation = borderangle, 1, 100, linear, loop
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    new_is_master = true
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = on
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
device:usb-optical-mouse- {
    sensitivity = 0.1
}

device:elan-trackpoint {
    sensitivity = 0
}


# Example windowrule v1
# windowrule = float, ^(kitty)$
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more


# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
# bind = $mainMod, Return, exec, alacritty
bind = $mainMod, Return, exec, kitty -1
bind = $mainMod SHIFT, W, exec, librewolf
bind = $mainMod SHIFT, S, exec, grim -g "$(slurp)" /home/vymvn/screenshots/last_screenshot.png && wl-copy -t image/png < /home/vymvn/screenshots/last_screenshot.png && dunstify -A 'swappy -f /home/vymvn/screenshots/last_screenshot.png','Open editor' "Screenshot taken" "Copied to clipboard" -i /home/vymvn/screenshots/last_screenshot.png | bash # Screenshot select
# bind = $mainMod SHIFT, S, exec, XDG_CURRENT_DESKTOP=Sway flameshot gui # Screenshot select (flameshot)
bind = $mainMod, Print, exec, grim /home/vymvn/screenshots/last_screenshot.png && wl-copy -t image/png < /home/vymvn/screenshots/last_screenshot.png && dunstify -A 'swappy -f /home/vymvn/screenshots/last_screenshot.png','Open editor' "Screenshot taken" "Copied to clipboard" -i /home/vymvn/screenshots/last_screenshot.png | bash # Screenshot
bind = $mainMod SHIFT, Q, killactive, 
bind = $mainMod ALT, M, exit, 
bind = $mainMod, E, exec, thunar
bind = $mainMod, V, togglefloating, 
bind = $mainMod, Space, exec, /home/vymvn/.config/rofi/launchers/type-3/launcher.sh
# bind = $mainMod, Space, exec, wofi
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, R, togglesplit, # dwindle
bind = $mainMod, F, fullscreen, 1
bind = $mainMod SHIFT, F, fullscreen, # Full full screen
bind = $mainMod, M, fakefullscreen,
bind = $mainMod SHIFT, X, exec, swaylock --indicator
bind = $mainMod SHIFT, P, exec, wlogout

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Move focus with vim keys
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

# Resize windows
bind = alt, r, submap, resize
submap = resize
binde = ,right, resizeactive,25 0
binde = ,l, resizeactive,25 0
binde = ,left, resizeactive,-25 0
binde = ,h, resizeactive,-25 0
binde = ,up, resizeactive,0 -25
binde = ,k, resizeactive,0 -25
binde = ,down, resizeactive,0 25
binde = ,j, resizeactive,0 25
bind = ,escape, submap, reset
submap = reset

# Move windows
bind = $mainMod SHIFT, H, movewindow, l
bind = $mainMod SHIFT, L, movewindow, r
bind = $mainMod SHIFT, K, movewindow, u
bind = $mainMod SHIFT, J, movewindow, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
